# ========================================
# Environment Configuration Template
# ========================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to git (it's in .gitignore)

# ========================================
# LLM Configuration (for Cloud Deployment)
# ========================================
# Get your free API key from: https://console.groq.com
# Leave empty for local development (will use Ollama automatically)
GROQ_API_KEY=your_groq_api_key_here

# ========================================
# Database Configuration (for Cloud Deployment)
# ========================================
# For local development, these are optional (uses defaults)
# For Railway/Cloud deployment, set these in platform settings

# Database host (default: localhost)
DB_HOST=localhost

# Database username (default: root)
DB_USER=root

# Database password (default: empty)
DB_PASSWORD=

# Database name (default: case_studies_db)
DB_NAME=case_studies_db

# ========================================
# Deployment Notes
# ========================================
#
# LOCAL DEVELOPMENT:
# - No .env file needed
# - Uses Ollama (localhost:11434)
# - Uses MySQL (localhost)
#
# RAILWAY DEPLOYMENT:
# - Set GROQ_API_KEY in Railway dashboard
# - Optionally set DB_* variables if using Railway MySQL
#
# RENDER DEPLOYMENT:
# - Set GROQ_API_KEY in Render dashboard
# - Set DB_* variables if using external MySQL
#
